#include <stdio.h>

/**
 * main - to print program name
 * @argc: arguremnet pased 
 * @argv: argument vector
 * Return: return zero
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
